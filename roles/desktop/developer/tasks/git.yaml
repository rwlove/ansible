- name: Install git
  package:
    name: git
    state: present

- name: Install gitconfig
  become_user: "{{USER}}"
  copy:
    src: "files/gitconfig"
    dest: "{{HOME}}/.gitconfig"
    owner: "{{USER}}"
    group: "{{USER}}"
    mode: '600'
