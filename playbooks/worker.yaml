- hosts:
    - worker2
  roles:
    - role: ../roles/common/etckeeper
    - role: ../roles/desktop/developer
    - role: ../roles/desktop/terminal
    - role: ../roles/kubernetes/worker
#    - role: ../roles/kubernetes/network/ovn-kubernetes

  vars:
    USER: root
    HOME: /root
