- hosts: 127.0.0.1
  connection: local
  roles: 
    - role: ../roles/media
    - role: ../roles/desktop/terminal
    - role: ../roles/desktop/developer
  vars:
    USER: rwlove
    KODI_CONFIG_DIR: /home/rwlove/.kodi